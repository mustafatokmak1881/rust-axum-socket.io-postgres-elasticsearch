HOST=0.0.0.0
PORT=3000

APP_ORIGIN=http://localhost:3000
COOKIE_SECURE=false

RUST_LOG=info,redis=warn

# Primary datastore (auth, lobbies, entitlements, match meta)
REDIS_URL=redis://127.0.0.1:6379

# Google Cloud Console OAuth
GOOGLE_CLIENT_ID=replace_me
GOOGLE_CLIENT_SECRET=replace_me

# Optional Stripe (cosmetics / XP boost only — never combat power)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# STRIPE_PRICE_FLAG_GOLD=
